@page
@using PriceMonitorData
@inject ItemPriceRepository db

Deleting...

@functions {
    public async Task<IActionResult> OnPost([FromForm(Name = "name")]string name, [FromForm(Name = "url")] string url)
    {
        await db.DeleteItem(name, url);

        return Redirect("~/");
    }
}
