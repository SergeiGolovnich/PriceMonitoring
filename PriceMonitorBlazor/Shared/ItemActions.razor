<button class="btn btn-danger" @onclick=@(async () => await OnRemoveClick.InvokeAsync(Item))>Remove</button>

@code {
    [Parameter]
    public PriceMonitorData.Item Item { get; set; }
    [Parameter]
    public EventCallback<PriceMonitorData.Item> OnRemoveClick { get; set; }
}
